import '@testing-library/jest-dom'
import * as React from 'react'

import { render as renderizar, screen } from '@testing-library/react'

import { Header } from '../src/components/Header'

describe('Header', () => {
  test('Deve fazer as chamas dos pokemons', () => {

  })
})